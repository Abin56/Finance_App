<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- SMS Inbox feature: on-demand read of the device SMS inbox for
         review/manual-conversion only. Deliberately no RECEIVE_SMS — there
         is no background/live SMS listener, only an explicit user-triggered
         scan (see lib/features/sms_inbox/data/sms_inbox_repository.dart). -->
    <uses-permission android:name="android.permission.READ_SMS"/>
    <!-- SMS Inbox feature: lets the app prompt the user to exempt itself from
         Doze/App Standby battery restrictions. Needed because OEM background-
         freezing (e.g. Samsung's "Optimised" battery mode) silently prevents
         NotificationCaptureListenerService's callback from ever running, even
         though the listener stays registered — see MainActivity's
         requestBatteryUnrestricted handler. Removed for the `play` flavor,
         same rationale as READ_SMS and the notification listener service
         above. See PLAY_STORE.md > "SMS Inbox and the Play build". -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <application
        android:label="FlowFi"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: re-arm scheduled reminders after
             reboot/app update, since exact alarms don't survive either. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- SMS Inbox feature: captures notification text from the device's
             default SMS/RCS app so RCS Business Messaging alerts (which never
             reach content://sms — see NotificationCaptureListenerService's
             doc comment) can still be scanned. Removed for the `play` flavor
             via src/play/AndroidManifest.xml, same rationale as READ_SMS
             above: Google Play's Restricted Permissions policy treats
             notification-listener access the same way it treats SMS access
             for a non-default-handler app. See PLAY_STORE.md > "SMS Inbox
             and the Play build". -->
        <service
            android:name=".NotificationCaptureListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
